include "reference"

server {
  base-url = "http://localhost:8082"
  port = 8082
  use-https = false
  environment = "dev"
}

db.url = "jdbc:mysql://localhost:3307/sftv2"

# Remove SSL keystore path so TLS context uses system default (not used anyway with use-https=false)
ssl = null

login.secret = "dev-secret-123"

# Uncomment and fill in to use real Google OAuth instead of dev bypass:
# google.client-id = "your-client-id"
# google.secret = "your-secret"
